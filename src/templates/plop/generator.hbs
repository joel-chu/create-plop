// {{name}} generator
export default function {{camelCase name}}Generator (config = {}) {
  return function (plop) {
    plop.setGenerator('{{camelCase name}}', {
      description: '{{description}}',
      prompts: [],
      actions: []
    })
  }
}
